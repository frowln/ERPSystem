package com.privod.platform.modules.hr.domain;

/**
 * Вид медицинского осмотра (Приказ Минздрава №29н от 28.01.2021).
 */
public enum MedicalExamType {
    /** Предварительный — при приёме на работу. */
    INITIAL,
    /** Периодический — в установленные сроки. */
    PERIODIC,
    /** Внеплановый (внеочередной) — по направлению работодателя или врача. */
    EXTRAORDINARY
}
